---
const year = new Date().getFullYear();

const links = [
  { label: 'GitHub',   href: 'https://github.com/pavelc4' },
  { label: 'LinkedIn', href: 'https://linkedin.com/in/pavelc4' },
  { label: 'Email',    href: 'dimasdwiariyanto24' },
];
---

<footer class="bg-gray-900 text-white border-t-4 border-bocchi-pink py-12 md:py-16 px-6 md:px-12">
  <div class="max-w-7xl mx-auto">

    <div class="flex flex-col md:flex-row justify-between items-start md:items-end gap-8">

      <div>
        <p class="font-oswald text-4xl md:text-6xl font-bold tracking-tighter text-white mb-2">
          pavelc4.
        </p>
        <p class="font-mono text-xs text-gray-500 uppercase tracking-widest">
          Dimas Dwi Ariyanto — Backend Engineer
        </p>
      </div>

      <nav aria-label="Social links">
        <ul class="flex flex-col sm:flex-row gap-4 sm:gap-8" role="list">
          {links.map(link => (
            <li>
              <a
                href={link.href}
                target="_blank"
                rel="noopener noreferrer"
                class="font-oswald uppercase tracking-widest text-sm text-gray-400 hover:text-bocchi-pink transition-colors flex items-center gap-1 group"
              >
                {link.label}
                <span class="transition-transform group-hover:translate-x-0.5 group-hover:-translate-y-0.5">↗</span>
              </a>
            </li>
          ))}
        </ul>
      </nav>
    </div>

    <div class="mt-12 pt-6 border-t border-white/10 flex flex-col sm:flex-row justify-between items-center gap-4">
      <p class="font-mono text-[10px] text-gray-600 uppercase tracking-widest">
        © {year} pavelc4. Built with Astro + Svelte + Bun.
      </p>
      <p class="font-mono text-[10px] text-gray-600 uppercase tracking-widest">
        // SYSTEM CORE BUILDER //
      </p>
    </div>

  </div>
</footer>